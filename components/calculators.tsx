'use client'
import {useMemo,useState} from 'react'
function monthly(principal:number,annual:number,years:number){const r=annual/100/12,n=years*12;if(!r)return principal/n;return principal*(r*Math.pow(1+r,n))/(Math.pow(1+r,n)-1)}
export function MortgageCalculator(){const [price,setPrice]=useState(700000),[down,setDown]=useState(20),[rate,setRate]=useState(6.5),[years,setYears]=useState(30);const loan=Math.max(0,price*(1-down/100));const p=useMemo(()=>monthly(loan,rate,years),[loan,rate,years]);const interest=p*years*12-loan;return <CalcCard title="Mortgage calculator"><Field l="Home price" v={price} set={setPrice} prefix="$"/><Field l="Down payment %" v={down} set={setDown}/><Field l="Interest rate %" v={rate} set={setRate}/><Field l="Term (years)" v={years} set={setYears}/><Result label="Estimated monthly principal + interest" value={p}/><div className="grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-mist p-4">Loan amount<br/><b>${loan.toLocaleString()}</b></div><div className="rounded-xl bg-mist p-4">Total interest<br/><b>${interest.toLocaleString(undefined,{maximumFractionDigits:0})}</b></div></div></CalcCard>}
export function RefinanceCalculator(){const [balance,setBalance]=useState(500000),[oldRate,setOldRate]=useState(7.25),[newRate,setNewRate]=useState(5.75),[years,setYears]=useState(25);const old=monthly(balance,oldRate,years),nw=monthly(balance,newRate,years),save=old-nw;return <CalcCard title="Refinance calculator"><Field l="Current loan balance" v={balance} set={setBalance} prefix="$"/><Field l="Current rate %" v={oldRate} set={setOldRate}/><Field l="New rate %" v={newRate} set={setNewRate}/><Field l="Remaining term (years)" v={years} set={setYears}/><Result label="Estimated monthly savings" value={save}/><p className="text-xs text-slate-500">Illustrative principal-and-interest comparison only. Closing costs, taxes, insurance and fees are not included.</p></CalcCard>}
function CalcCard({title,children}:{title:string;children:React.ReactNode}){return <div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="font-serif text-2xl font-semibold">{title}</h3><div className="mt-6 space-y-4">{children}</div></div>};function Field({l,v,set,prefix}:{l:string;v:number;set:(n:number)=>void;prefix?:string}){return <label className="block text-sm font-medium">{l}<div className="mt-1 flex items-center rounded-xl border border-slate-200 px-3"><span>{prefix}</span><input type="number" value={v} onChange={e=>set(Number(e.target.value))} className="w-full bg-transparent px-2 py-3 outline-none"/></div></label>};function Result({label,value}:{label:string;value:number}){return <div className="rounded-2xl bg-navy p-5 text-white"><div className="text-xs uppercase tracking-wider text-slate-300">{label}</div><div className="mt-1 text-3xl font-bold">${Math.max(0,value).toLocaleString(undefined,{maximumFractionDigits:0})}<span className="text-sm font-normal"> / month</span></div></div>}
