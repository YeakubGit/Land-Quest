'use client'
import Link from 'next/link';import {Menu,X,Phone} from 'lucide-react';import {useState} from 'react'
export default function Header(){const [open,setOpen]=useState(false);const links=[['Buy','/buy'],['Rent','/rent'],['Foreclosures','/foreclosure'],['Sell','/sell'],['About','/about'],['Reviews','/reviews'],['Calculators','/calculators']];return <header className="sticky top-0 z-50 border-b border-slate-200/70 bg-white/95 backdrop-blur"><div className="container-shell flex h-20 items-center justify-between"><Link href="/" className="font-serif text-xl font-bold tracking-tight text-navy">LAND QUEST <span className="text-gold">HOME</span></Link><nav className="hidden items-center gap-6 lg:flex">{links.map(([t,h])=><Link key={h} href={h} className="text-sm font-medium text-slate-700 hover:text-navy">{t}</Link>)}<Link href="/contact" className="rounded-full bg-navy px-5 py-2.5 text-sm font-semibold text-white">Contact</Link></nav><button aria-label="Open menu" className="lg:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t bg-white lg:hidden"><nav className="container-shell flex flex-col py-4">{links.map(([t,h])=><Link onClick={()=>setOpen(false)} className="border-b py-3" key={h} href={h}>{t}</Link>)}<Link onClick={()=>setOpen(false)} href="/contact" className="mt-3 rounded-full bg-navy px-5 py-3 text-center font-semibold text-white">Contact</Link></nav></div>}</header>}
