import Image from 'next/image';import {Star} from 'lucide-react';import {urlFor} from '@/lib/sanity';import type {Review} from '@/types'
export default function Reviews({reviews}:{reviews:Review[]}){return <div className="grid gap-6 md:grid-cols-3">{reviews.slice(0,3).map(r=><figure key={r._id} className="rounded-2xl border border-slate-200 bg-white p-6"><div className="flex gap-1">{Array.from({length:r.rating||5}).map((_,i)=><Star key={i} size={16} fill="currentColor" className="text-gold"/>)}</div><blockquote className="mt-5 text-lg leading-7 text-slate-700">“{r.review}”</blockquote><figcaption className="mt-6 flex items-center gap-3">{r.photo?<Image src={urlFor(r.photo).width(80).height(80).url()} alt="" width={40} height={40} className="h-10 w-10 rounded-full object-cover"/>:<div className="grid h-10 w-10 place-items-center rounded-full bg-navy text-sm font-bold text-white">{r.clientName?.[0]}</div>}<div><div className="text-sm font-semibold">{r.clientName}</div><div className="text-xs text-slate-500">{r.propertyType||'Real estate client'}</div></div></figcaption></figure>)}</div>}
