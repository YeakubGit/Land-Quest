'use client'
import {useRouter} from 'next/navigation';import {Search} from 'lucide-react';import {useState} from 'react'
export default function SearchBar(){const r=useRouter();const [q,setQ]=useState('');const [type,setType]=useState('buy');return <form onSubmit={e=>{e.preventDefault();r.push(`/${type}${q?`?q=${encodeURIComponent(q)}`:''}`)}} className="grid gap-2 rounded-2xl bg-white p-2 shadow-xl md:grid-cols-[150px_1fr_auto]"><select value={type} onChange={e=>setType(e.target.value)} className="rounded-xl bg-mist px-4 py-3 text-sm font-semibold"><option value="buy">Buy</option><option value="rent">Rent</option><option value="sell">Sell</option><option value="foreclosure">Foreclosure</option></select><div className="flex items-center gap-2 rounded-xl bg-mist px-4"><Search size={18} className="text-slate-400"/><input aria-label="Search location" value={q} onChange={e=>setQ(e.target.value)} className="w-full bg-transparent py-3 text-sm outline-none" placeholder="Neighborhood, borough, city or ZIP"/></div><button className="rounded-xl bg-navy px-6 py-3 font-semibold text-white">Search</button></form>}
