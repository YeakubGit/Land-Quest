import Image from 'next/image';import Link from 'next/link';import {BedDouble,Bath,Square} from 'lucide-react';import {urlFor} from '@/lib/sanity';import type {Property} from '@/types'
export default function PropertyCard({p}:{p:Property}){const img=p.images?.[0]?urlFor(p.images[0]).width(900).height(650).fit('crop').url():'/placeholder-property.jpg';return <article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><Link href={`/properties/${p.slug}`}><div className="relative aspect-[4/3] bg-slate-100"><Image src={img} alt={p.images?.[0]?.alt||p.title} fill className="object-cover" sizes="(max-width:768px) 100vw, 33vw"/><span className="absolute left-4 top-4 rounded-full bg-white/95 px-3 py-1 text-xs font-bold">{p.status}</span></div><div className="p-5"><div className="text-2xl font-bold text-navy">{p.price?`$${p.price.toLocaleString()}`:'Price upon request'}</div><h3 className="mt-1 font-serif text-xl font-semibold">{p.title}</h3><p className="mt-1 text-sm text-slate-500">{[p.neighborhood,p.borough,p.city].filter(Boolean).join(' · ')}</p><div className="mt-4 flex gap-4 text-xs text-slate-600">{p.bedrooms!=null&&<span className="flex items-center gap-1"><BedDouble size={15}/>{p.bedrooms} bd</span>}{p.bathrooms!=null&&<span className="flex items-center gap-1"><Bath size={15}/>{p.bathrooms} ba</span>}{p.sqft!=null&&<span className="flex items-center gap-1"><Square size={15}/>{p.sqft.toLocaleString()} sf</span>}</div></div></Link></article>}
