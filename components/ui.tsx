import Link from 'next/link'
export function Button({href,children,variant='primary'}:{href?:string;children:React.ReactNode;variant?:'primary'|'outline'|'light'}){const c=`inline-flex items-center justify-center rounded-full px-5 py-3 text-sm font-semibold transition ${variant==='primary'?'bg-gold text-ink hover:brightness-95':variant==='light'?'bg-white text-ink hover:bg-mist':'border border-white/30 text-white hover:bg-white/10'}`;return href?<Link className={c} href={href}>{children}</Link>:<button className={c}>{children}</button>}
export function SectionTitle({eyebrow,title,copy}:{eyebrow?:string;title:string;copy?:string}){return <div className="max-w-2xl"><div className="mb-2 text-xs font-bold uppercase tracking-[.22em] text-gold">{eyebrow}</div><h2 className="font-serif text-3xl font-semibold tracking-tight md:text-5xl">{title}</h2>{copy&&<p className="mt-4 leading-7 text-slate-600">{copy}</p>}</div>}
