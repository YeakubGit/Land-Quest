import type {MetadataRoute} from 'next';import {sanityFetch} from '@/lib/fetch';import {propertiesQuery} from '@/lib/queries';import type {Property} from '@/types';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';const ps=await sanityFetch<Property[]>(propertiesQuery);return['','buy','rent','foreclosure','sell','calculators','about','reviews','contact','properties'].map(p=>({url:`${base}/${p}`.replace(/\/$/,''),lastModified:new Date()})).concat(ps.map(p=>({url:`${base}/properties/${p.slug}`,lastModified:new Date()})))}
