import {sanityFetch} from '@/lib/fetch';import {reviewsQuery} from '@/lib/queries';import type {Review} from '@/types';import Reviews from '@/components/reviews';import {SectionTitle} from '@/components/ui'
export default async function ReviewsPage(){const reviews=await sanityFetch<Review[]>(reviewsQuery);return <main className="container-shell py-16"><SectionTitle eyebrow="Client reviews" title="What our clients say." copy="Reviews are managed in Sanity Studio and can be published or hidden without touching the website code."/><div className="mt-10"><Reviews reviews={reviews}/></div></main>}
