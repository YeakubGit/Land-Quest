import ContactForm from '@/components/contact-form';import {SectionTitle} from '@/components/ui';import {sanityFetch} from '@/lib/fetch';import {settingsQuery} from '@/lib/queries'
export default async function Contact(){const s=await sanityFetch<any>(settingsQuery);return <main className="container-shell grid gap-12 py-16 lg:grid-cols-2"><div><SectionTitle eyebrow="Contact" title="Let's talk New York real estate." copy="Whether you’re buying, selling, renting or exploring an investment, send us a note."/><div className="mt-8 space-y-3 text-slate-600"><p>{s?.address||'New York, NY'}</p><p>{s?.phone||'(212) 555-0123'}</p><p>{s?.email||'hello@landquesthome.com'}</p></div></div><div className="rounded-3xl border p-7 shadow-sm"><ContactForm/></div></main>}
