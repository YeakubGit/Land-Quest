import {sanityFetch} from '@/lib/fetch';import {propertiesQuery} from '@/lib/queries';import type {Property} from '@/types';import PropertyGrid from '@/components/property-grid';import {SectionTitle} from '@/components/ui'
export default async function Page(){const all=await sanityFetch<Property[]>(propertiesQuery);const properties=all.filter(p=>p.categories?.includes('foreclosure'));return <main className="container-shell py-16"><SectionTitle eyebrow="Foreclosure" title="Foreclosure properties in New York." copy="Updated listings managed directly by the Land Quest Home team."/><div className="mt-10"><PropertyGrid properties={properties}/></div></main>}
